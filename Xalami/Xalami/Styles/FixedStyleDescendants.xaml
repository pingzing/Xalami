<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:root="clr-namespace:Xalami"
             x:Class="Xalami.Styles.FixedStyleDescendants">
  <!--Put styles that depend on any of the "Fixed" styles from App.xaml in here. Those
    don't get initialized properly until runtime, and so any styles that are BasedOn
    them cannot be defined in App.xaml. This ResourceDictionary gets merged into the primary
    Application ResourceDictionary in App.xaml.cs after the Fixed styles are initialized.-->
  
  <!--Your styles that are BasedOn a "Fixed" style go here-->
  
</ResourceDictionary>